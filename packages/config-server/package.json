{
  "name": "@openclaw/config-server",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --port 18080",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "test": "nuxt prepare && tsc --noEmit"
  },
  "dependencies": {
    "@openclaw/shared": "workspace:*",
    "naive-ui": "^2.39.0",
    "vue": "^3.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "nuxt": "^3.14.0",
    "typescript": "^5.6.0"
  }
}
